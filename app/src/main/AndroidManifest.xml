<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.topmarks3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TopMarks3">
        <activity android:name=".RecoverData"></activity>
        <activity android:name=".Layout" />
        <activity android:name=".Notifications" />
        <activity android:name=".CourseView" />
        <activity android:name=".CreateCourse" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.TopMarks3.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>